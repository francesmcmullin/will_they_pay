<%= form_with(model: result) do |form| %>
  <% if result.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(result.errors.count, "error") %> prohibited this result from being saved:</h2>

      <ul>
        <% result.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :percent_covered, style: "display: block" %>
    <%= form.text_field :percent_covered %>
  </div>

  <div>
    <%= form.label :days_waited, style: "display: block" %>
    <%= form.number_field :days_waited %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
