<p style="color: green"><%= notice %></p>

<h1>Plans</h1>

<div id="plans">
  <% @plans.each do |plan| %>
    <div>
      <%= render plan %>
      <p>
        <%= link_to "Show this plan", plan %>
      </p>
    </div>
  <% end %>
</div>

<%= link_to "New plan", new_plan_path %>
